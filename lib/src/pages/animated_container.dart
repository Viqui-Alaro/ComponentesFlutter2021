
import 'package:flutter/material.dart';


class sborrar extends StatelessWidget {
  const sborrar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}